package classes;

public class StaticMethods {
    /*
        if a method doesn't use any instance variables or methods, it probably should be a static method
     */
    public static void printHello() {
        System.out.println("Hello!");
    }
}
